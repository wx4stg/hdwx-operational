# Destination directory to send files
targetDir="" # type absolute path between quotes
# Time (in hours) to purge plots
purgePlotsAfter="" # type integer between quotes
# true to enable backwards compatibility with JSImageViewer upstream, false to save time/resources (ideal, but only works with Sam's fork of JSImageViewer)
backwardsCompatibility=false
# Path to your conda installation. This is usually ~/miniconda/ for miniconda, $condaRootPath/ for miniforge+mamba, ~/miniforge/ for miniforge, or ~/micromamba/ for micromamba.
condaRootPath=""
# Name of the conda environment to generate products. If you don't know what this means/if you're following the install guide, just set it to "HDWX"
condaEnvName=""
